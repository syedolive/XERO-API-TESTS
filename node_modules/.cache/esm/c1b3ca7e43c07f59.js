let xero;_43c‍.w("../xero_client/client",[["default",["xero"],function(v){xero=v}]]);var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', async (req, res, next) => {
  try{
    const token = await xero.apiCallback(req.url);
    res.send(token);
  }catch(e){
      _43c‍.g.console.log(e);
  }
  
});

module.exports = router;
